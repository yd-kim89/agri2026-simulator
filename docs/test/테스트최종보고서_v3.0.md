# 테스트 리포트 (최종) — overedge · v3.0 · 2026-07-27

대상 커밋: 0520428 · 환경: production + **staging(신규 실행)** · baseUrl: https://overedge-phi.vercel.app
프로젝트: agri2026 매입 시뮬레이터(overedge) · 기준 PRD: [PRD v6-sync](../prd/PRD_overedge_v6-sync.md)

> **v3.0 변경점:** v2.0에서 제외했던 스테이징 mutation(M-1~M-3)을 **실행 완료**했다.
> Supabase 스테이징 프로젝트(`farmtempo-staging`, `ctqalibngdtsnqkydqer`) 확보 →
> `staging_schema.sql` 적용 → Vercel Preview(staging 브랜치 전용 env) 배포 →
> `npm run test:staging` **3/3 통과**. 이로써 설계된 44케이스 전량 실행·전량 통과.

## 요약
| 구분 | 수 |
|---|---:|
| 전체 케이스(설계) | 44 |
| 자동화 실행 | **44** |
| ✅ 정상 통과 | **44** |
| 🔴 회귀(문서 ✅ ↔ 실패) | 0 |
| ⚪ 미실행(제외) | **0** |

- E2E 14 케이스 × 2 뷰포트(desktop·mobile) = 28 — **07-27 회귀 재실행 전부 통과** (27.1s)
- 단위(Vitest) **27/27 통과** — 07-27 재실행
- 스테이징 mutation **M-1~M-3 = 3/3 통과** (9.4s, 07-27 최초 실행)
- **앱 소스 결함 0** · 회귀 0 · 운영 DB 무접촉

## 스테이징 mutation 실행 상세 (신규)
| ID | 내용 | 결과 |
|---|---|---|
| M-1 | POST /api/simulate → `simulation_log` INSERT (`logged:true`) | ✅ pass (1.1s) |
| M-2 | /api/history read-back — 방금 넣은 익명 기록 조회 | ✅ pass (3.8s) |
| M-3 | UI 익명 시뮬레이션 → 이력 탭 라이브 기록 노출 | ✅ pass (3.6s) |

**실행 환경 검증 절차:**
1. 스테이징 Supabase(`ctqalibngdtsnqkydqer`) — 운영(`yabuqapajz…`)과 별개 프로젝트임을 확인
2. `staging_schema.sql` 적용 후 두 테이블 존재·데이터 0건 확인 (REST 200)
3. Vercel env `NEXT_PUBLIC_SUPABASE_URL/ANON_KEY`를 **Preview(staging 브랜치) 스코프로만** 추가 — 운영 env 무접촉
4. staging 브랜치를 main(0520428)으로 fast-forward push → Preview 배포(21s, Ready)
5. 배포본 `/api/history`가 `count:0` 응답 → 스테이징 DB 바인딩 확인 후 테스트 실행
6. 실행 후 스테이징 `simulation_log`에 3행(배추 100·양파 313·대파 77) 기록 확인 — 운영 DB 오염 없음

## 정적 게이트 / 회귀 체크 (v2.0에서 유지)
- ✅ 핵심 로직 이원화 — 시나리오 ②(예측 h=7) / ③(27년 이벤트 계수) 출처 불혼합
- ✅ 배추 역방향(suppress) — cost=null·"선매입 억제"
- ✅ API 계약 — 200 정상 / 400(item·qty·JSON) / 404(미지 품목)
- ✅ 정직성 라벨 — "참고용"(conf≤2)·"거래 기능 개발 중·시나리오 값"·"데모/라이브" 상시
- ✅ graceful degradation — 참고값 모드 배너 + 시뮬레이션 지속
- ✅ Supabase 키 클라이언트 노출 없음(publishable/anon 키만, service_role 미사용)

## 케이스별 결과
| ID | 파일 | 우선순위 | 판정 |
|---|---|---|---|
| P0-1 시세보드 로드 | p0-core | P0 | ✅ |
| P0-2 시뮬레이터·배추 역방향 | p0-core | P0 | ✅ |
| P0-3 사과 급등 절감액 | p0-core | P0 | ✅ |
| P0-4 API 계약(200/400/404) | p0-core | P0 | ✅ |
| P0-5 로그인·이메일 검증 | p0-core | P0 | ✅ |
| P1-1 잘못된/빈 수량 방어 | p1-exceptions | P1 | ✅ |
| P1-2 새로고침 상태 초기화 | p1-exceptions | P2 | ✅ |
| P1-3 탭 내비게이션 무결 | p1-exceptions | P1 | ✅ |
| P1-4 graceful 참고값 모드 | p1-exceptions | P1 | ✅ |
| P1-5 마진 계산 | p1-exceptions | P1 | ✅ |
| P1-6 직거래 견적·파트너 미모집 | p1-exceptions | P1 | ✅ |
| P1-7 이력·판정 필터 | p1-exceptions | P1 | ✅ |
| P1-8 양파(none) 이벤트 | p1-exceptions | P1 | ✅ |
| P1-9 근거 펼침 | p1-exceptions | P2 | ✅ |
| 단위(Vitest) simulate 27건 | __tests__/simulate | P0 | ✅ (27/27) |
| M-1 API→simulation_log INSERT | staging-mutation | P0 | ✅ **(신규 실행)** |
| M-2 /api/history read-back | staging-mutation | P0 | ✅ **(신규 실행)** |
| M-3 UI 익명 시뮬 → 라이브 노출 | staging-mutation | P1 | ✅ **(신규 실행)** |

## 배포 판정
### ✅ READY
설계된 44케이스 전량 실행·전량 통과. 제외 항목 0. 앱 결함 0, 회귀 0, 운영 DB 무접촉.
QA 자동화 시스템 v1.0(설계)→v2.0(하네스)→**v3.0(전량 실행) 완결**.

---
*작성: Claude Code (SuperClaude / quality-engineer) · AgriY&D / overedge 최종 테스트 리포트 v3.0 (2026-07-27)*
